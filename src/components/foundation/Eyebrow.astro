---
interface Props {
  class?: string;
  [key: string]: unknown;
}
const { class: className = '', ...attrs } = Astro.props;
---

<p {...attrs} class={`text-eyebrow font-medium uppercase text-ink-low ${className}`}>
  <slot />
</p>
