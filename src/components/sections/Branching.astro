---
// Section 2 (04 §3 v5): the head rides the pin — eyebrow/H2/intro render inside
// the pinned screen so they stay at the top while the scrub plays. The whole
// section lives in the island; this wrapper only provides the anchor.
import BranchScrub from './branch/BranchScrub.tsx';
---

<section id="branch" class="relative">
  <BranchScrub client:visible />
</section>
